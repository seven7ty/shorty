{
  "private": true,
  "type": "module",
  "name": "shorty",
  "version": "1.0.0",
  "description": "My personal Cloudflare worker serving as a URL shortener, among other things.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Paul Przybyszewski <paul@przybyszewski.me>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2",
    "itty-router": "^2.1.9"
  }
}
